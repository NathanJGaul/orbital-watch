# orbital-watch
Real-Time Telemetry Dashboard — a full-stack app (React + .NET Core API) streaming mock satellite state vectors via SignalR, with SQL Server schema, Redis cache, and a Three.js ground track.
