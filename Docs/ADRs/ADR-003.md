# ADR-003: AssSingleton for IConnectionMultiplexer

Capture: `ConnectionMultiplexer` is designed to be a long-lived singleton. Creating per-request instances would exhaust TCP connection. Contrast with DbContext (scoped).

Consequences: any service that injects `IConnectionMultiplexer` must itself be singleton-safe (no scoped dependencies held longer than a request).